# ARC signing on forwarded / auto-retransmitted mail (docs/05 §4.3, phase 2) so
# the original authentication survives the hop.
enabled = true;
sign_authenticated = true;
sign_local = true;
selector = "soura";
path = "/var/lib/rspamd/dkim/$domain.$selector.key";
use_domain = "header";
