# Add the headers the inbound-process worker and clients rely on for placement
# (docs/05 §4.1 step 6). Rspamd decides Inbox vs Spam; the worker just files it.
use = ["x-spamd-bar", "x-spam-level", "authentication-results", "spam-header"];
extended_spam_headers = true;
authenticated_headers = ["authentication-results"];
