# Greylisting for borderline-score inbound only (docs/05 §4.1). Never greylist
# authenticated submission or high-reputation senders.
servers = "redis:6379";
greylist_min_score = 4;
timeout = 300;                 # 5 min initial defer
expire = 86400;
whitelist_domains_url = [];
